// Placeholder entry point — see ../../README.md. Not yet wired to a
// backend API.

import SwiftUI

@main
struct CelShadedGeneratorApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Cel-Shaded-Generator — scaffold, not yet implemented.")
        }
    }
}
